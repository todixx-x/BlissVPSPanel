version: '3.8'

services:
  blisspanel:
    build: .
    container_name: blisspanel
    ports:
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /:/host:ro
    environment:
      - PORT=8080
    restart: unless-stopped
